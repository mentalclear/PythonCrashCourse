dimensions = (200, 50)
print(dimensions[0])
print(dimensions[1])

#  dimensions[0] = 250  # Tuple object doesn't support item assignment

# Looping through tuple
for dimension in dimensions:
    print(dimension)
